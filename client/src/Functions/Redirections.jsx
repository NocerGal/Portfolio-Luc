export const redirectSectionDescription = () => {
  const sectionPresentation = document.querySelector('#description');
  sectionPresentation.scrollIntoView({ behavior: 'smooth' });
};
export const redirectSectionContact = () => {
  const sectionPresentation = document.querySelector('#contact');
  sectionPresentation.scrollIntoView({ behavior: 'smooth' });
};
export const redirectSectionRealisations = () => {
  const sectionPresentation = document.querySelector('#realisations');
  sectionPresentation.scrollIntoView({ behavior: 'smooth' });
};
